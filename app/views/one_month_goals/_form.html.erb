<h1>Enter your new goal here fat ass</h1>

<!-- adding remote new -->
<%#= form_for([@user, @one_month_goal], remote: true) do |f| %>
<%= form_for([@user, @one_month_goal]) do |f| %>
  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
