<!DOCTYPE html>
<html>
<head>
  <title>Wynning</title>

</head>

        <h1 class="wynning">WYNNING</h1>
      </span>

      <% if current_user %>
      <div class="">
        <nav class="">
          <a class="e">Welcome, <%= current_user.first_name.capitalize %>  |  <%= link_to "Log out", logout_path, method: "delete" %></a>
          <% else %>
          <a class="m"><%= link_to "Login", new_session_path %>  |  <%= link_to "Signup", new_user_path %></a>
          <% end %>
        </nav>
      </div>
    </div>
  </div>
</div>


<%= yield %>

</body>
</html>
